# Alembic Migrations

Place Alembic revision files here. Name revisions with timestamp + short description (e.g., `20240925_1500_create_trade_tables`).

Run migrations with `uv run alembic upgrade head`. Keep autogenerated SQL readable and align with specs in `docs/specs/trade_store.md`.
