# Social Templates

Store Jinja2 templates for social messages here. Name files by channel + tier (e.g., `discord_premium_entry.j2`).

Document required context variables at the top of each template. Keep sensitive data (account numbers) out of rendered content.
