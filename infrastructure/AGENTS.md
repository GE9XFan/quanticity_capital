# Infrastructure Planning

Holds deployment artifacts once containerization/systemd configs are introduced. For now this is a placeholder to keep repo layout stable.

## TODO
- `docker/` – Dockerfiles and compose definitions aligning with module boundaries.
- `terraform/` or `gcp/` – future infrastructure-as-code if we extend to Google Cloud.

Document any operational scripts here as they land.
